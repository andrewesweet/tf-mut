# M5 spec review — 16 September 2026

Adversarial review of the M5 milestone spec (revision 1 of the working draft), followed by three bounded re-reviews of the repairs (revisions 2, 3 and 4), all posted as comments on the M5 spec issue. The reviewer worked against tree `d57f2cd` with Terraform v1.15.8, built HEAD, and reproduced its lifecycle, preview, cache and refusal claims offline on `terraform_data`; no network-gated measurement, OpenTofu run or repository edit occurred. **Verdict as delivered on revision 1: "not ready for an implementation agent — five critical contract failures, seven major gaps, and two minor wording/source issues."** Revision 2: "not ready — one critical and five major repair findings remain (R1–R6)". Revision 3: "Not ready — two major repair findings (R7, R8) remain; one minor test-protocol correction (R9) can be carried into its ticket." Revision 4: "**Ready for publication and ticketing; no blocking R7/R8 item remains.**"

All twenty-three findings accepted — fourteen on revision 1 (three with chosen-option modifications), six on revision 2 (one with modification), three on revision 3 (none). The spec issue's body is revision 4; the dispositions below are the record, in the order the rounds ran. The final section carries the evidence quotations and rationale that stood beside the decisions in the working draft and were moved out of the published body for size; nothing there adds a requirement the body lacks.

The reviewer's operational clause, added to the standing checklist question and carried into every M5-0 decision rule: "**what observation would make this decision rule fire, and does that observation establish the exact claim the rule permits us to publish?**"

## Dispositions (revision 2)

Independent review of revision 1, posted as the first review comment on the M5 spec issue, 2026-09-16, against tree `d57f2cd`. Verdict as delivered: "not ready for an implementation agent — five critical contract failures, seven major gaps, and two minor wording/source issues." All fourteen findings accepted; three with modifications named below; none rejected. Where a repair changes a decision rule, the new rule is quoted from revision 2 so that an implementation agent can run the measurement and apply it without asking.

**Counts: accepted 11 (C1, C3, C4, C5, M1, M2, M4, M5, M7, m1, m2); accepted with modification 3 (C2, M3, M6); rejected 0.**

| # | Sev | Finding (compressed, quoting the review) | Disposition and the exact text changed |
| --- | --- | --- | --- |
| C1 | CRIT | "'Payload difference', 'canonical observable difference' and 'baseline-green killing test' have been conflated"; the rule "admits `LC-PREVENT-DESTROY-FLIP` on evidence of an unusable red baseline"; four probes "cannot prove that *no* Terraform test construct could observe an operator"; `oracle.go` "handles a proven delta **before** `!mutation.Projects`" | **Accepted, the recommended option.** Revision 1's rule — "an operator observable under at least one of (a)–(d) ships in M5a … an operator observable under none is **withdrawn from the catalogue**" — is replaced by admission on a **kill witness** ("a suite under which the original module is baseline-green and the mutant is `Killed` or `KilledByError` — recorded with the run-block shape and the assertion or error that did the killing"), over shapes (a)–(e) with (e) apply/apply added, four recorded columns (raw paths, assertion-reachable paths, canonical delta, witness), and the non-admission outcome reworded: "its design-table row is annotated 'no kill witness under shapes (a)–(e), Terraform v1.15.8' … and the row reopens on a witness. This is prove-or-withdraw as **bounded admission policy** … it is not a claim that no Terraform construct could ever observe the operator." The oracle ordering point becomes a measured item: "M5-0.1 records, for every shape, whether the canonical delta lies solely on such members … *Claim licensed*: that member joins the unreachable set in M5a, with the reproduction joining `just gate`". Structural-state precedence not reopened. Verified in the tree: `internal/engine/oracle.go:212-214` (`if proven(delta)` precedes the switch at `:231`); `internal/fingerprint/payload.go:196-205` (`unreachableMembers` has eight entries; `actions`/`action_reason` absent) |
| C2 | CRIT | "Reusing the old census' execution posture either refuses untested provider-bearing modules because there is no mocked suite, or bypasses gates"; "Copying diagnostic evidence also creates a new pre-harvest publication path"; "the M4.5 wrong-program failure again" | **Accepted with modification: the recommended option, entered only through a static stage.** M5-0.5 is now two stages. Stage 1 "runs no Terraform; the existing `todos` posture". Stage 2 "runs only if stage 1 finds ten or more opportunities" and "stages the candidate **through the existing characterise pipeline**: the effective staged suite with one planned mock per provider configuration, both pre-execution safety gates decided statically before any run, the JSON floor, the sandbox, the environment control of standing rule 5, and the M4.5 M8 redaction rule for every failed attempt"; "A module the gates refuse is an opportunity with outcome *refused*, published as such, never executed under an opt-in the recipe's environment variable does not grant." The two acceptance pairs the review asked for are named: zero invocations on a gate refusal (`TestNoTerraformRunPrecedesAStagedGateRefusal`'s shape) and secret-only-in-failed-attempt (`TestASecretInAFailedAttemptReachesNoArtefact`'s shape). The modification: the reviewer's alternative (static census) is stage 1, so that when opportunities are scarce no new execution path is built at all |
| C3 | CRIT | `BOOL-LITERAL-FLIP` "precedes `PACK-FLIP`. Thus an identical boolean rewrite enabled in `standard` wins over the pack rewrite"; "only one scalar pack/rule slot available"; "The unchanged algorithm erases the policy provenance of exactly the boolean faults used to motivate the seeded pack" | **Accepted, the recommended option** (preserve content identity; publish deterministic origin metadata for every contributing entry). Revision 1's "A pack mutant's report row carries the operator, the pack name and the source rule identifier" and the schema rule "present exactly when `tier` is `pack`" are replaced by: "M5c keeps the algorithm and the mutant identity exactly as they are … and adds **origins**: every `(operator, pack, entry)` whose rewrite produced the surviving bytes is recorded on the surviving mutant, in deterministic order, whichever operator owns the row"; schema: "`mutants[].origins` … present exactly when at least one pack entry produced the mutant's bytes". Witness counting: "**pre-deduplication** (bytes produced) and **post-deduplication** (owns a row), both published". Verified in the tree: `internal/mutation/generate.go:584-608` (`sortMutants` by identifier after `generatedRank`), `:611-631` (`deduplicate` by file and content digest) |
| C4 | CRIT | Oasis's units "are **mutants**"; the M4.5 both-denominators measurement is "**modules**"; "An implementer cannot put an assertion-kill count over 'all pinned modules' and call it a mutation score"; the threshold "preserves the strongest claim in precisely the branch where it remains unproven" | **Accepted, the recommended option** (narrow now; separate module-admission rates and mutant-level equations). New text: "The 'directly comparable' wording is narrowed **now**, in the implementing change and independent of any count, to 'a side-by-side evaluation on Oasis's axes with stated limitations'"; M5d: "**Two tables, two units** … *Module admission* … *Mutant level, in Oasis's units*: generated = mutants `preview` produced over **every** pinned module (`preview` is never refused by the floor) … **unscorable** = mutants generated on modules whose row outcome is a refusal or `red-baseline`, tabulated by row outcome — the analogue of Oasis's 61"; "Aggregation is stated: pooled sums over `scored` modules (Oasis's method), plus per-module rows and the unweighted per-module median"; Out of Scope: "**The phrase 'directly comparable'.**" The `preview`-based unscorable analogue is the drafter's addition and is flagged for the re-review (flag 6). Verified in the tree: `TestAPreviewIsNeverRefusedByTheFloor` at `internal/engine/jsonfloor_test.go:277` |
| C5 | CRIT | "carrying it is not **executing** it"; `listTodos` "runs no Terraform at all — not even the version gate"; "An all-command flavour gate breaks the installed inspection loop; labelling its report as a binary that 'executed' fabricates provenance"; "'Every command with a pack' also contradicts the inapplicable-flag refusals" | **Accepted, the recommended option** (an explicit command/case table; no-execution commands keep their contract and omit driven identity). New text, M5b: "Binary-driving commands — `run`, `preview`, `suggest`, `characterise`, `curate` — decide the flavour … `todos` keeps its contract verbatim … and its report carries **no** driven-binary block and an empty `terraform_version`, which is what it carries today"; "The permitted pre-refusal probe is exactly one `version -json`"; "Four test shapes are distinguished: successful execution …; version-only refusal (one invocation, named); pre-version refusal … (zero invocations); and no-binary (`todos` succeeds with no `PATH` binary at all)". Testing: "with and without a pack on the three commands that accept one". Out of Scope: "**Changing `todos` to execute anything**, including a version probe". Verified in the tree: `internal/engine/engine.go:219-231` (`todos` dispatched before the version gate, `listTodos(listed, settings, "")`), `internal/engine/todos.go:143-146` |
| M1 | MAJ | Two HEAD runs "produce `baseline.duration_ms=23` and `24`; their reports are not byte-identical"; "the literal refusal test fails after its necessary version probe"; "One pinned release proves that release, not the earliest release supporting a feature" | **Accepted, the recommended option** (pinned deterministic projection with a same-binary control; version-only probes allowed; pin as minimum; divergence acceptance table). New text: "comparing each report … under the **pinned deterministic projection**: the report with `baseline.duration_ms`, every per-mutant and per-run duration, the driven-binary block, `terraform_version`, and the two residuals the structural programme already recorded … removed, and nothing else. States, diagnoses, findings, evidence, metrics, gate outcomes, refusal messages and exit codes are **never** normalised. The projection is proven sufficient first by a **same-binary control**"; "the pin is the minimum supported version"; the triage into "named divergence fixture … prerequisite gap … oracle disagreement", each with its licensed claim. Revision 1's "byte-identical (except the driven-binary block)" is gone |
| M2 | MAJ | "No loader, pack registration setting, form schema or pack selection exists at HEAD"; the agent "must invent the public file grammar, name/path registration, duplicate/unknown handling, ordering and CLI/config composition …"; pack bytes are not in the snapshot composition | **Accepted, the recommended option** (normative tables for loader, entry, selection and error contracts; snapshot rules; every row pinned through the seam). New text: the "**pack contract (review M2), normative tables**" paragraph — literal-only parsing, seven entry fields, accepted attribute forms, evidence including type compatibility, `pack "NAME" { file = "PATH" }` registration with reserved names, selection by name with exit-2 refusals, composition with tier and operator selection, duplicate and conflict rules, and "the selected pack names and, for a user pack, the file's bytes join the resolved-configuration dimension of the cache key and the input-closure digest the write protocols re-check … a changed user-pack file forces the full population under `--since`". Verified in the tree: `internal/engine/cache.go:309-315` (`resolvedConfiguration` is a resolved-field string; no configuration bytes) |
| M3 | MAJ | "Domain classification is a proxy, not a verified resolution count"; judgement points "with **no typed candidate at all**"; the harness "counts native `.tf` text only"; "Nine repairable gaps out of nine would trigger removal under the current rule"; "The high-yield branch leaves #82's acceptance unmet unless it remains explicitly open" | **Accepted with modification.** Recommended elements taken: "**Verified resolution** means the repaired candidate plans green in the staged scenario; nothing else counts"; opportunities "**deduplicated by module and input**"; classification "*typed candidate refused by the static evaluator*, *no typed candidate at all*, or *undecidable constraint*"; outcomes "attempted, verified, refused, no-candidate, unmeasured (operational), with invocation count and wall time per module"; the JSON stratum "published as **unmeasured**, never as zero" when absent; "#82 stays open, linked to a build issue" on a positive result; on amendment "the preference order in `docs/design/characterisation.md` §3.2 and the design record of issue #75 are amended". Modification: the thresholds ten and one-in-four are **kept** as labelled policy — "The two thresholds are explicit product policy, not measured values" — with the rationale the review asked for, and the small-sample case is disposed of by scoped wording rather than by declaring low yield: "over N corpus modules, no input — or fewer than ten — reached the point where the fourth rung would begin". Revision 1's "the census is nearly free" is withdrawn; costs are published |
| M4 | MAJ | "The start table supplies each slice's own measurement only, not earlier-slice gates"; "Following the explicit table implements the closure-only posture the quoted disposition rejected"; "M5-0.3 requires the pack mechanism that M5c cannot start before the measurement" | **Accepted, the recommended option** (one explicit dependency graph). Revision 1's "M5a starts when M5-0.1 has published; M5b when M5-0.2 has; M5c when M5-0.3 has; M5d when M5-0.4 has" is replaced by the **Sub-scope blocking** table: kinds (measurement, production, prototype, record), start conditions that chain on earlier gates ("M5b … after M5-0.2 closes **and M5a closes**"; "M5c.1 … after M5b closes"; "M5-0.3 … after M5c.1 closes"; "M5d … **last**"), and M5-0.5 attached to its deliverables (exit gate 6). M5c is split into M5c.1 mechanism and M5c.2 seeded pack so the admission measurement has a mechanism to run against |
| M5 | MAJ | "A missing/unavailable provider, archive or init failure, malformed source, unsupported payload, per-mutant execution failure, or timeout need not be any of these six refusals"; "network/setup failures are not deterministic module properties" | **Accepted, the recommended option** (total row-outcome table with precedence, operational and incomplete outcomes, zero-mutant case, retry policy, authority). New text: the **Row outcomes** paragraph — `operational` "retried once, then published with the error text as **a fact about this run, not the module**", `unsupported-construct`, `real-infrastructure`, `unsandboxed-effects`, `no-suite`, `red-baseline`, `scored-incomplete` ("excluded from the pooled headline"), `scored` ("an authoritative population, the same value `curate` and `--write-baseline` require"); "Precedence is that order"; "A `scored` module with zero mutants is a row of zeros, kept"; "Publication never aborts on an operational row"; portable assertions restricted to refusal classes being deterministic and verdict identity over `scored` modules |
| M6 | MAJ | "The meaning-change admission contradicts 'additive'"; "retaining an old schema file is not an output-version negotiation mechanism"; "The driven-binary property's exact name, requiredness and absence semantics are not normatively tabulated" | **Accepted with modification.** The field/presence/vocabulary/migration table is added, with two rejection cases and `todos`-without-binary validated. "A new binary emits only 2.4.0; keeping 2.3.0 published is for old documents, not an output-version negotiation." Modification against the review's alternative ("preserve `terraform_version`'s existing meaning"): `terraform_version`'s meaning is **widened and documented** — "the driven binary's reported `terraform_version` field, or `""` when no binary executed" — rather than preserved, because for a `tofu` run the field would otherwise be false, and rather than emptied, because `todos` already uses `""` to mean "no binary executed" (`internal/engine/engine.go:231`) and a `tofu` run did execute one. The version stays 2.4.0; the review agrees the repository "does **not** by itself establish that every such change must be numbered 3.0.0". Flagged for the re-review (flag 8) |
| M7 | MAJ | "Retaining all of M5-0 in M5 while moving its .5 census to M5.5 describes two different cuts"; "two milestones need not mean two corpora"; recommends "keeping one milestone … with corpus pinning/admission as explicit early pre-work and final benchmark publication last" | **Accepted, the recommended option.** Further Notes: "**One milestone** … corpus pinning and admission as explicit early pre-work (M5-0.4 stage 1 starts immediately), final benchmark execution and publication last. The #82 census stays owned by M5 in M5-0.5 … The M5.5 proposal is withdrawn." The dependency graph places M5d last |
| m1 | MIN | `preview --tier standard` and `--tier deep` "each return 224 mutants with identical mutant arrays and zero `deep` rows"; "The five lifecycle identifiers are design-table identifiers, not enabled catalogue entries" | **Accepted.** Problem Statement now reads: "`--tier deep` is accepted and, in the catalogue's own words, 'currently enables nothing beyond `standard`' — the five lifecycle identifiers are design-table rows, and the enabled catalogue has zero Tier 4 entries." |
| m2 | MIN | Overrides "4, 6 and 7 are summaries rather than full source quotations"; "a literal claim that all seven were quoted rather than paraphrased would be false" | **Accepted.** Overrides 4, 6 and 7 now carry the decisive excerpts: §8's full mining paragraph; all three per-assertion-provenance sources; #84's first comment in full. The heading says "each with its decisive source excerpt" |

**Rule-2 note.** Every repair above was unreviewed design until the bounded re-review passed it; eight rule-2 flags named where the repairs introduced new decisions rather than restating the review's.


## Dispositions (revision 3)

Bounded re-review of revision 2 ("Re-review of revision 2" in the posted review), 2026-09-16, against tree `d57f2cd` and revision-2 SHA-256 `3dfeaccad8389062817d9e28ed05f50578197070f724933c6bf990cc113b0238`. Verdict as delivered: "not ready — one critical and five major repair findings remain (R1–R6 below)." The re-review reran the forty lifecycle observations and twelve assertion tests and added offline CLI checks of preview failures, fresh-versus-cached reports, safety-refusal invocation logs and a smoke-tier action-path delta; it asked for no change to "the accepted kill-witness admission principle, single-milestone choice, no-binary `todos`, narrowed comparison claim, policy thresholds as labelled policy, or documented 2.4.0 meaning change", and none is made.

**Counts: accepted 5 (R1, R2, R3, R4, R5); accepted with modification 1 (R6, the reviewer's alternative taken); rejected 0.** Three residuals the re-review attached to revision-2 dispositions (C1's gate wording, M1's "minimum" wording, M4's rerun-at-M5b note) are repaired in the same pass and listed after the table. Every tree claim the re-review made was re-verified before disposing of it.

| # | Sev | Finding (compressed, quoting the re-review) | Disposition and the exact text changed |
| --- | --- | --- | --- |
| R3 | CRIT | `preview` on three builtin-only modules — no suite, `import`, malformed source — "exit 2 … None yields a report or mutant count"; "`TestAPreviewIsNeverRefusedByTheFloor` proves only that one floor exemption, not immunity from these failures"; "Treating missing counts as zero or silently omitting them would recreate the dishonest total C4 was meant to remove"; the table "omits source-parse failure and disagrees with its own glossary about unsupported payload versions" | **Accepted, the recommended option** (population availability as a separate fact; sum only known counts; state how many are unknown; no complete mutant denominator and no Oasis-equivalent unscorable count; a row outcome for source-parse failure; one payload-version spelling; precedence in preview's real stage order). Revision 2's "generated = mutants `preview` produced over **every** pinned module (`preview` is never refused by the floor)" is replaced by: "*Mutant level, in Oasis's units, over known populations only*: generated = the sum of known populations, stated as 'over K of N modules, with N−K populations unknown' — **no complete mutant denominator is claimed when any population is unknown**"; the vocabulary gains **Population availability**: "**known** (a successful `preview` produced it) or **unknown** (no `preview` report exists, with the row outcome as the reason). A count is never inferred, defaulted to zero or omitted silently". Row outcomes gain `unparseable-source`; `unsupported-payload-version` is its own outcome at stage (6) and is removed from `operational`; precedence is the pipeline's seven stages with `preview` placed at stage (5) after `init`, so "a population is **known** for every row that reached stage (6), and **unknown** for `operational` rows at (1) or (5) and every refusal at (2), (3) or (4)". The revision-2 claim "the analogue of Oasis's 61" is withdrawn in the text: "the document publishes Oasis's 61 unscorable **mutants** and tf-mut's unknown-population **modules** as different facts and says why they differ". Verified in the tree: `internal/engine/engine.go:348-355` (`prepareWorkRoot` refuses zero run blocks for every grading command, preview included); `internal/engine/baseline.go:51-60` (`warmUp` — init and schema — runs before the preview shortcut returns) |
| R1 | MAJ | "The existing unreachable-member filter is **global**, not a lifecycle-only policy"; a smoke-tier `EXT-RESOURCE-DELETE` survivor "publishes `resource_changes[terraform_data.subject[0]].change.actions[0]` in its delta"; "Dropping the member globally necessarily changes non-Tier-4 report evidence/fingerprints"; "`engine.go:719–735` applies module/conditional `NoCoverage` before the projecting-only static-equality shortcut: non-projecting does not mean exempt from every static state" | **Accepted, the recommended option** (defer a global projection change unless an admitted operator needs it; if needed, a separately specified slice with path scope, preserved precedence, cross-operator measurement and recorded evidence changes). Revision 2's "*Claim licensed*: that member joins the unreachable set in M5a, with the reproduction joining `just gate`" is replaced by: "M5-0.1 therefore **records** reachability per operator, shape and path, and changes nothing. *Observation*: an **admitted** operator, under its witnessed shape, has a canonical delta lying solely on unreachable members. *Claim licensed*: an **oracle slice is specified in its own issue** … and it is not built in M5. *Observation*: only an unadmitted operator … has such a delta. *Claim licensed*: the finding is published … and nothing in the oracle changes." M5a's "never statically classified, always executed" is replaced by "the static-equality shortcut, which is for projecting operators only, never classifies it `Unobservable`; module-level and conditional `NoCoverage` keep their precedence over every mutant, admitted lifecycle mutants included"; and "**M5a changes the canonical projection for no member**". The invariance claim is made exact because no oracle member changes: "identical under matched cache-off legs with `baseline.duration_ms` excluded and nothing else excluded". Verified in the tree: `internal/fingerprint/payload.go:196-224` (one global `unreachableMembers` table and `reachable`); `internal/engine/engine.go:715-735` (the two `NoCoverage` cases precede `staticallyUnobservable`) |
| R2 | MAJ | "The named staged test actually permits `versionInvocation` (`characterise_test.go:719–737`), just as `jsonfloor_test.go:150–170` does"; both `run` and `characterise` refusing an unsandboxed provisioner "log only `-chdir=<scratch-module> version -json`"; "L202 also says binary-driving commands decide flavour before other work, so its zero-probe safety row contradicts its own execution order" | **Accepted, the recommended option** (permit exactly the existing version probe on floor, provider and effects refusals; forbid init, schema and test before them; reserve zero invocations for pre-version configuration and flag refusals and no-binary commands; carry the wording into the prototype's acceptance pair). Revision 2's "pre-version refusal — the gates and the floor — (zero invocations)" is withdrawn by name and replaced by three refusal kinds: "**pre-version** — configuration and flag refusals … — **zero** invocations; **version-only** — the safety gates, the JSON floor, the staged provider gate and the flavour's version and prerequisite gates — **exactly one** `version -json` and no `init`, `providers schema` or `test` …; and **no-binary** — `todos` succeeds with no `PATH` binary at all." The prototype's pair now reads "a gate refusal produces **exactly one** `version -json` invocation and nothing else — the shape `TestNoTerraformRunPrecedesAStagedGateRefusal` actually asserts, which permits the version probe". Verified in the tree: `internal/engine/characterise_test.go:719-737` (`if invocation != versionInvocation`); `internal/engine/engine.go:234-243` (`checkVersion` precedes `discovery.DiscoverWith`) |
| R4 | MAJ | "`origins[].entry` has no declared wire representation"; "two equivalent upstream rules in `security-aws` cannot both be loaded as entries" under the revision-2 duplicate rule; "A file could use labelled entry blocks or a list of objects and satisfy the current prose"; the `from` match predicate was dropped; "orthogonality to tier does not decide whether CLI `--pack` replaces or merges configured packs"; `terraform_data.input` "has type `dynamic`"; the red proof should "disable origin aggregation, not merely change ownership order" | **Accepted, the recommended option, with the alternative's author-supplied IDs as the identity mechanism** (the re-review offered it as an acceptable alternative; it is the simplest thing that gives a stable identity, a rename contract and a duplicate rule that keeps upstream provenance). New contract text: "containing only `entry \"ID\" { … }` blocks"; "the label `ID` is author-supplied, `[a-z0-9-]+`, unique within the pack … an origin's wire identity is the pair `(pack, entry)`"; "two entries of one pack with identical `(resource_type, attribute, form, from, to)` but different labels are **allowed** … and both become origins"; per-form load checks (`flip` boolean and opposite; `replace` same kind, `to` differs, no-op is an error; `widen-cidr` sentinel); site matching restored as "a **single literal token equal to `from` after HCL literal decoding**"; "a schema type of `dynamic` accepts any literal kind (the offline witness, `terraform_data.input`, is `dynamic`) and a concrete type must match"; composition "**merged as a union** (the additive rule the file already applies to reporters …), deduplicated by name". Origins: "**sorted and deduplicated by `(pack, entry)`** … aggregation is independent of the winner". Red proof: "**disables origin aggregation**, not merely the sort order". The schema row for `origins` names the wire fields. Verified in the tree: `internal/config/config.go:45-46` ("Reporters are the configured outputs, merged additively with the flags") |
| R5 | MAJ | Two same-binary runs under the default cache posture: "`{selected:1,omitted:0,cached:0,fresh:1}`" then "`{… cached:1,fresh:0}`; provenance changes from `fresh` to `cached` and gains `cache_key`. None is in the exclusion list"; "Cross-flavour cached keys must differ by the newly mandated flavour dimension"; "'A random mutant value' gives no JSON path or predicate"; "The standard before/after-M5 comparison also encounters the deliberate new schema/block changes" | **Accepted, the recommended option** (matched initial cache state per leg; cache-off for semantic comparisons and staged identical cold/warm transitions for cache tests; cache identity compared by relationship; exact paths and semantic comparators for volatile cases; authorised schema-addition differences for base/target). The revision-2 comparator — durations, binary identity, "Terraform's diagnostic order under `KilledByError` and a random mutant value, removed, and nothing else" — is replaced by two comparisons: "**The per-contract comparison is the gate itself** … **The whole-report survey** … runs under a **matched-state comparator**: both legs cache-off (`--no-cache`) …; the excluded paths are exactly `baseline.duration_ms` (the schema's only duration field), `terraform_version` and `driven_binary`; and any further path that differs is not deleted but **classified** by the same-binary control … given either an exclusion with its reason or a semantic comparator (an order-insensitive set for `KilledByError` diagnostics; a mask-aware comparison for a value the baseline mask marks volatile); a path in the protected set … is never excluded". Cache identity: "two same-flavour legs under an explicitly staged cold-then-warm transition must show equal `cache_key` values and `fresh` then `cached` provenance; two cross-flavour legs … unequal keys". Base/target at M5d: "authorises exactly the schema-addition differences: `schema_version`, `driven_binary`; `origins` is absent with no pack". "Random mutant value" is dropped as a rule; it is a candidate the classification protocol names by path if the control finds it. Verified in the tree: `docs/schema/report-2.3.0.json:60,71` (`duration_ms` is the schema's only duration field); no `rand.` call in `internal/mutation` or `internal/engine` production code, so the residual is a payload value, not a diff |
| R6 | MAJ | "Stage 1 can find ten or more gaps while stage 2 attempts **zero**"; "a per-input candidate also cannot plan green until the other inputs are supplied"; "the finite candidate sequence/attempt attribution still needs to be pinned before a yield is measured"; "For the 1–9 branch, the stated reopen condition is already true of the very sample being closed" | **Accepted with modification: the reviewer's alternative taken** ("specify a module/scenario-level experiment and denominator rather than promise per-input attribution it cannot isolate"), combined with the recommended insufficient branch and new-evidence reopen condition. New text: "The **unit is the module scenario**, not the input …: for a module with k unresolved inputs the prototype supplies a candidate for every one of them in one staged scenario"; the table "**pinned before execution** … keyed by the **Terraform function named in the expression at the diagnostic's source range** … never by the diagnostic's summary text"; outcomes "**Verified** … **refuted** …, with the input the diagnostic names published and its siblings recorded as *blocked*; *no-candidate* …; *unmeasured* …"; "The **measured cohort** is the verified plus refuted module scenarios"; "**Decision rule, total over the cohort.** *Observation*: the cohort is empty … *Claim licensed*: **insufficient**: the counts and costs are published, the repair-yield decision stays open, #82 stays open with no build issue"; the two policy thresholds kept and applied only to the cohort, "the cohort size is always published beside the rate"; the small-sample closure's reopen condition is "a later pinned corpus revision — a new manifest digest — whose stage-1 census reports ten or more opportunities". The modification against the recommended option: attribution is at module level with per-input naming only where Terraform names the input, rather than a per-input experiment. Verified in the tree: `internal/engine/characterise.go:140-150` (an unresolved judgement point leaves no executable suite, the invariant the re-review cites) |

**Residuals repaired in the same pass.** *C1 gate wording*: `gate-m5` now re-executes each witness as "mutant **`Killed` or `KilledByError`**, whichever the witness recorded", matching the vocabulary's definition. *M1 wording*: "the pin is the minimum supported version" is replaced by "the only supported release" in M5-0.2 and Out of Scope. *M4 rerun*: "The M5-0.2 survey is pre-work; **the M5b gate reruns every comparison against the tree as it stands at M5b**" is added to M5-0.2's decision rule and to the dependency graph's M5b row.

**Rule-2 note.** The revision-3 rule-2 flags named where these repairs introduced new decisions; the final bounded re-review was scoped to them.


## Dispositions (revision 4)

Re-review of revision 3 ("Re-review of revision 3" in the posted review), 2026-09-16, against tree `d57f2cd` and revision-3 SHA-256 `e36a4a4e781be33e9e465328a817f684b730c3111e4aac192d5023fc9f0b1216`. Verdict as delivered: "**Not ready — two major repair findings (R7, R8) remain; one minor test-protocol correction (R9) can be carried into its ticket.**" R1–R6 and the three named residuals are recorded as resolved; "The accepted milestone shape, policy thresholds, schema decision and kill-witness admission were not reopened", and none is reopened here.

**Counts: accepted 3 (R7, R8, R9 — R9 carried into the M5b comparator ticket with its wording applied now); accepted with modification 0; rejected 0.** Both R7 and R8 take the reviewer's recommended option; nothing run in this pass contradicted them, and the two tree facts they rest on were re-verified.

| # | Sev | Finding (compressed, quoting the re-review) | Disposition and the exact text changed |
| --- | --- | --- | --- |
| R7 | MAJ | "`preview --reporter json` on the preserved provisioner-refusal fixture succeeds with **six mutants**; `run --no-cache --reporter json` on the same source exits 2 for unsandboxed effects. `engine.go:275–286` deliberately bypasses safety gates for preview"; "there is no public gate-only pause into which the benchmark can insert a `PreviewRequest`"; "The previous review proved *some previews fail*, not that *all grading refusals make previews fail*"; the known-but-unscored list "omits" `unsupported-payload-version` and baseline-stage `no-suite` | **Accepted, the recommended option** (an executable sequence of complete public requests; availability from the actual preview report; known counts preserved when a separate grading invocation refuses; the bucket over every non-scored row with a known count; the overbroad attribution removed; the Testing Decisions sentence reconciled). Revision 3's "`preview` runs at stage (5) … a population is **known** for every row that reached stage (6), and **unknown** for `operational` rows at (1) or (5) and every refusal at (2), (3) or (4)" is replaced by: "For each module run **two complete public invocations, always both, in this order**: first `preview --no-cache --reporter json`, whose own outcome decides **population availability** …; second `run --no-cache --tier standard --reporter json`, whose own outcome decides the **row outcome**. Neither invocation's result is inferred from the other's." The vocabulary now reads "Availability is decided by the preview invocation's outcome alone, never inferred from the grading invocation's row outcome"; the row-outcome table is re-titled "ordered by the grading invocation's own stages" and ends "Revision 3's derivation of availability from the grading stages is withdrawn"; the bucket reads "mutants counted by a successful `preview` on **every** module whose row outcome is not `scored` — `real-infrastructure`, `unsandboxed-effects`, `red-baseline`, baseline-stage `no-suite`, `unsupported-payload-version`, `scored-incomplete` and `operational` alike"; the attribution "the re-review showed that every refusal before stage (6) leaves the population unknown" is removed; the prior-art sentence now reads "why a module the grading run refuses at the gates can still have a known population from its own `preview` invocation, and why availability is read from that invocation and never from the grading outcome". Verified in the tree: `internal/engine/engine.go:273-284` (`if settings.mode != previewMode { checked, err := checkSafety(...) }` — preview skips the gates, with the comment "a preview executes nothing. Refusing a preview would only hide the population from the person deciding whether to accept the risk") |
| R8 | MAJ | "The diagnostic's range is `tests/candidate.tftest.hcl:4:12–20`, the literal **`"tf-mut"`**, not `can(cidrnetmask(var.cidr))`. The validation location appears in prose in `detail`"; "Stage 1 is static, so it also supplies no real Terraform plan diagnostic to bootstrap the lookup"; "`tf-mut todos` on the same module publishes `variable: cidr`, `constraint: can(cidrnetmask(var.cidr))` and `range: main.tf:4:21–47`" | **Accepted, the recommended option** (the judgement point's constraint expression and range as the initial lookup source; the runtime diagnostic range distinguished from it; a fail-closed mapping for retries with an unmappable outcome; the table pinned before the experiment). Revision 3's "the table is keyed by the **Terraform function named in the expression at the diagnostic's source range**" is replaced by: "**Initial candidate selection reads the judgement point as `tf-mut todos` already publishes it** — the variable, its **constraint expression** and the constraint's range in the module source … — and keys the table on the Terraform function names appearing in that constraint expression"; "**A Terraform runtime diagnostic is a different thing and is never the lookup key** … Prose is not parsed"; "For the **one retry** after a red plan, the failed input is identified fail-closed from the diagnostic's structured fields alone — a `snippet.values[].traversal` naming `var.<name>`, or a structured range that lies inside one of the module's own `validation` blocks — … a diagnostic that maps to no input is *unmappable*, no retry is made, and the scenario stays refuted". The cohort and insufficient branch are unchanged, as the re-review asked. Verified in the tree: `TestTodosListsTheOpenJudgementPointsWithTheirEvidence` (the `todos` surface publishes constraint and range; `just gate-m45`) and the review's E4 stream (`snippet.values[0].traversal` = `var.cidr`), the only structured field that names the input |
| R9 | MIN | "the cold report has `execution: fresh` and **no `cache_key`**; warm reports two and three both carry `c471fafff3604572`. This is intentional (`cache.go:494–496` …)"; "In the cache ticket use **cold → warm → warm**" | **Accepted, carried into the M5b comparator ticket, wording applied now.** Revision 3's "two same-flavour legs under an explicitly staged cold-then-warm transition must show equal `cache_key` values" is replaced by: "on reports that carry a key: a fresh report carries `execution: fresh` and no `cache_key` — the key is published only on a hit — so the staged transition is **cold → warm → warm**: within one flavour the cold report shows `fresh`, both warm reports show `cached`, and the two warm reports' `cache_key` values are equal; across flavours the warm keys differ … No key publication is added to fresh reports to satisfy the wording." The `gate-m5` case reads "under a staged cold → warm → warm transition on the two warm reports". Verified in the tree: `internal/engine/cache.go:494-496` (`CacheKey` set only on a rehydrated hit) |

**Also applied from the re-review's flag 7 note**: the `gate-m5` union case now requires "both a configured and a flag-selected pack contributing, and a name given in both deduplicated once".

**Rule-2 note.** Flags 8 and 9 named the two repairs of this revision; the final narrow pass was scoped to them and found no blocking item.


## Rationale and evidence moved out of the specification body

The specification body published on the issue is revision 4 with the passages below moved here so that the body fits the platform's size limit. Every decision, normative sentence and table row of revision 4 stays in the body verbatim; what follows is the evidence quotation, review rationale and justificatory commentary that stood beside those decisions in the working draft, listed by the body section it was removed from. Nothing here adds a requirement the body lacks; where a passage below contains a rule, the same rule stands in the body in shorter words.

### M5-0.1

- The review established by running it that payload difference, canonical delta and baseline-green killing test had been conflated; the measurement re-runs those observations through this repository's fixtures and extends them:
- This is prove-or-withdraw as **bounded admission policy**; it is not a claim that no Terraform construct could ever observe the operator. On the review's evidence the prior is: `LC-IGNORE-DROP`, `LC-IGNORE-ALL` admitted on (b); `LC-REPLACE-TRIGGER-DROP` admitted on (e); `LC-PREVENT-DESTROY-FLIP` and `LC-CBD-FLIP` not admitted unless this measurement finds a witness the review did not.
- - **The reachability measurement, and the narrower claim it licenses.** The canonical projection drops eight members no assertion can read — `provider_name`, `schema_version`, `mode`, `depends_on`, `replace_paths`, `deposed_key`, `tainted`, `provider_config_key` — and the oracle diagnoses a proven delta **before** it consults the projection flag, so a mutant whose only difference is `change.actions` or `action_reason` would today be a survivor whose fix names a path no assertion can read. The filter is global and `actions` already appears in a smoke-tier survivor's delta, so dropping it would change existing report evidence and baseline fingerprints across every tier. M5-0.1 therefore **records** reachability per operator, shape and path, and changes nothing.
- *Observation*: only an unadmitted operator has such a delta. *Claim licensed*: the finding is published in the M5-0.1 document and nothing in the oracle changes. On the review's evidence the three admitted operators move an assertion-reachable value (`after.input`) or an identity (`id`) under their witnessed shapes, so the expected outcome is the second.

### M5-0.3

- — the shape the M3 spec review's C7 disposition demanded of the last opt-in catalogue: it "may widen `standard` only after published candidate counts, generated-mutant counts and per-pair invalid/error rates on a real module — a measured fault model, per standing rule 1". The builtin provider's schema describes `terraform_data.input` as `{"type":"dynamic","description_kind":"plain","optional":true}`, which is what the offline user-defined pack's evidence requirement rests on; that fact does not establish the AWS pack's applicability, type or fault-model claims, which is why admission runs on `aws-mocked`.

### M5-0.4

- The both-denominators rule from the M4.5 measurement applies to the module table verbatim: "a rate quoted only over the subset that survived is the posture `checkCuratePopulation` refuses in `curate` … and a measurement that holds its users to that standard publishes both numbers rather than the flattering one" — and it is applied to modules, never substituted for the mutant-level table.

### M5-0.5

- #82 asks: "A measurement, in the shape standing rule 4 requires: how often would a diagnostic-driven repair resolve an input that the three built rungs leave as a judgement point?" The measurement is two stages.
- and #82 closes under its own acceptance: "the preference order in issue #75 and in `docs/design/characterisation.md` is amended to the three rungs that exist, with the measurement as its justification. Either outcome closes this; what must not persist is a specification claiming four rungs over an implementation with three." The reopen condition
- The prototype does not run. The prior: over the M4.5 corpus every readable module had zero open judgement points, which warrants "not observed here" and nothing stronger.
- and the M4.5 M8 redaction rule for every failed attempt — "Redaction applies to **every** artefact: diagnostics, attempted values, generated comments, reports, terminal, non-executable TODO files".
- The two thresholds are explicit product policy, not measured values: ten is the module count of the corpus the last decision rule was stated over; one in four falsifies the design's own claim that a small table "would handle the bulk"; the cohort size is always published beside the rate so a reader weighs a small cohort for what it is.

### M5a

- , in the shape the M2 spec review's C5 disposition made normative: "per operator: accepted source forms, required type/schema evidence, coordinated rewrites, skip rules, expected error classification". "Kills when"
- **M5a changes the canonical projection for no member**; the reachability measurement and its conditional oracle slice are M5-0.1's. The product design's repair stands: "The state is assigned statically from the construct class, carries its fix … and sits in the denominator: an untested validation rule is a real finding, not noise to exclude".

### M5b

- - **Posture, quoted.** The first adversarial review's m15 disposition: "OpenTofu is a tested matrix, not a flag"; the design's open question 6: "treat OpenTofu as a tested matrix — its `test` command already diverges — rather than a `--engine` flag that implies parity". The `--engine terraform|tofu` row

### M5c

- - **Posture, quoted.** "Oasis-style semantic faults, kept deliberately separate from the language catalogue because they are provider-specific and their coverage of any given module is patchy by nature. Enabled per pack, never by default." and "Each pack entry follows Oasis's `(resource_type, attribute)` scoping model, which is the right call and should be adopted directly: an attribute name alone is not enough context to know whether mutating it models a real fault".
- The generator sorts same-site mutants by operator identifier — curated before generated — and drops a later mutant that rewrites the file to identical bytes; the design says so: "Deduplication is by mutated file content" and "the entry sorting earliest wins". Under that rule `BOOL-LITERAL-FLIP` precedes `PACK-FLIP`, so a boolean fault in `standard` owns the row and the pack's provenance would be lost. M5c keeps
- , and the maintainer's ruling is honoured as recorded: "configuration-narrowed populations from `.tf-mut.hcl` stay refused at configuration time for `curate` and `--until-dry`, exactly as the M4.5 spec review's C5 disposition states; only flag-based narrowing moved to the CLI parser as `errInapplicableFlag`". Pack selection
- , exactly the M3e posture: "Admission to `standard` remains a separate, evidence-carrying change: this document is the evidence, and the decision is deliberately not taken here".

### M5d

- - **Protocol, pinned in the M3 spec review's M5 shape**: "the gate pins fixture commit, exact diff, expected selected IDs and a non-zero minimum, cache-off state, tier and warm/cold protocol; asserts shared-verdict identity; publishes hardware and method; the portable regression assertion is selected-count plus factor-versus-baseline, with the 60-second figure a published measurement on named hardware, not a portable assertion". For the benchmark:
- Preview skips stage (3) and its refusals are its own (no run blocks declared, unsupported construct, unparseable source, or an operational failure of fetch, `init` or schema), so a module whose `run` is refused at the gates commonly has a known population; the two outcomes stay independent in every combination, including a preview that fails operationally while the later run succeeds.

### Schema

- The rule, quoted: "extending one is a minor schema version with the consumer contract documented, not a silently additive change. Changing a field's name or meaning means a new schema version and a new file". A new binary

### Testing

- **What makes a good test here, quoted from the two documents M5 inherits.** From the M4.5 implementation review: "assert the thing the contract is about, not the thing you expect to see when it holds — with two riders: prove the assertion can fail, and check whether the contract you are writing already exists two packages over"; and "when a mechanism has two ends, test the path between them, not each end". From the structural exit gate's lessons: "A specification that names a signature or a fixture states a hypothesis the tree gets to refute". From the review of this specification: "**what observation would make this decision rule fire, and does that observation establish the exact claim the rule permits us to publish?**" Tests assert
- **Prior art in the tree.** The three matrix-enforcement tests (note the mirror gate on the site test); `TestTheGeneratedCatalogueIsOptIn` and the `families` fixture (an opt-in catalogue behind a flag, proven not to touch the default population — the shape the pack opt-in case takes); the pinned-corpus harness with published rows and a decision rule; the inner-loop gate (a pinned protocol with portable assertions separated from published measurements); the base-versus-target comparisons the structural programme recorded and its list of residual nondeterminism; `TestTheDocumentedVocabularyMatchesTheBinary` (the design rows M5 retires must disappear from the documents it checks); `TestExportedEngineTypesDeclareNoSeamControls` (why the pack surface is production, not a seam control); `TestNoTerraformRunPrecedesAFloorRefusal` and `TestNoTerraformRunPrecedesAStagedGateRefusal` (a refusal asserted on the invocation log, permitting the one version probe); `TestASecretInAFailedAttemptReachesNoArtefact` (the redaction pair's shape); `TestNonTerraformClassChangesForceTheFullPopulation` (the `--since` rule a pack file joins); `TestAPreviewIsNeverRefusedByTheFloor`, together with the preview branch that skips the safety gates (why a module the grading run refuses at the gates can still have a known population from its own `preview` invocation, and why availability is read from that invocation and never from the grading outcome).

### Out of Scope

- - **Per-assertion provenance and `curate --apply`.** Deferred unchanged: "A per-assertion registry needs per-assertion digests written at generation time and is the prerequisite for any future `curate --apply`, which M4.5 deferred and this programme did not touch".
- - **Sharing the verification legs of scaffold promotion and `suggest --apply`.** Carried forward in the narrowed form the structural exit gate left it: "Whether the two *verification* legs should share more than they do, the record is silent; the question carries forward in that narrower form".

### Further Notes

- **One milestone.** Corpus pinning and admission are explicit early pre-work (M5-0.4 stage 1 starts immediately), final benchmark execution and publication come last, and the #82 census stays owned by M5 in M5-0.5, as the brief requires. An "M5.5 = M5d" cut was proposed in revision 1 and withdrawn on review, because the pack admission uses the scorable corpus and the #82 census sits in M5-0.

## Pattern note

From the review of revision 1: "This draft has learned to name measurements and quote inherited rules, but several measurements still prove a different proposition from the product promise: **a changed payload rather than a green suite that kills the mutant; a diagnostic domain rather than a verified repair; a generated form rather than a published pack-origin witness; a count of scorable modules rather than a comparable mutant population; a binary setting rather than a binary that executed.** The new mode then inherits a familiar contract in prose while bypassing its deciding boundary." From the re-review of revision 2: "the repair adds the right distinction, then overextends a nearby fact — floor exemption into universal preview, non-projecting into always executed, or a no-init safety gate into no version probe. Keep the narrower claim the executable evidence supports." From the re-review of revision 3: "two repairs selected the right concept but attached it to the wrong existing boundary — grading stages versus a separate preview, and a diagnostic location versus the constraint expression. Fix those references rather than adding new subsystems."

Round eight of adversarial review in this repository, and the sharpest fact in it: the offline prevent-destroy reproduction had an observable positive result under the draft's proposed gate, yet the product cannot grade its baseline. The M4.5 question — *which program is this evaluated against, and has every standing rule been re-asked against that program?* — gains the operational clause above.

## Standing rules re-asked against each new program

Rule 1 (no feasibility claim before a realistically-sized schema): the pack admission runs on `aws-mocked` (`hashicorp/aws` v6.60.0), not the null provider; the offline user-pack over `terraform_data.input` proves the mechanism, not the AWS pack. Rule 2 (a repair is unreviewed design): every decision that repaired a review finding went back for a bounded re-review before publication — three rounds, twenty-three findings, none rejected — and two the implementation review should re-ask first are the kill-witness admission predicate over shapes (a)–(e) and the two-invocation benchmark protocol. Rule 3 (run it): M5-0.1 and M5-0.2 exist for this reason alone, and M5-0.1's shape set was extended by the reviewer running it — the apply/apply shape (e) is the reviewer's. Rule 4 (a lever is unbuilt until its cost model is measured): no speed lever in M5; the benchmark publishes wall-clock only as a measurement; the repair prototype publishes its invocation counts and wall time, which #82 asks for and revision 1 called "nearly free" without measuring. Rule 5 (the tool controls the environment): re-asked for `tofu` in M5-0.2 as a tested strip-or-keep list, and for the prototype in M5-0.5, which hands Terraform a staged suite the gates have approved and nothing else.

## Overrides recorded (structural exit gate > #84 > M4.5 exit gate)

1. *`discovery.Attribute` and `hcl.Expression`*: #84 lists it open; the structural exit gate §9 records it "Answered: yes, by measurement (#111), contract (#115) and reading (#116)". Not in M5.
2. *Three write protocols*: #84 asks "One write protocol, or three?"; §9 records "Answered by #91 in the direction #85 argued: share the checked atomic-replace primitive only, never the preconditions (§3.5)" and "Whether the two *verification* legs should share more than they do, the record is silent; the question carries forward in that narrower form". Not in M5; the remainder is out of scope.
3. *#83 seam controls*: #84 says "twenty-four seam controls sit on the exported `engine.Config`"; §2.2 records "twelve `Seed*` fields plus two `Disable*` fields, fourteen in all" and all fourteen removed, with `TestExportedEngineTypesDeclareNoSeamControls` holding the line. Closed; M5 inherits the constraint.
4. *Validation mining*: #84 and 13-m45 §14 ask "Is the validation-mining rung worth its code? It fired zero times over the corpus"; §8 records: "It fired zero times and was reached four times over the M4.5 corpus. The programme changed one input to the question without answering it: after #116 the rung reaches `.tf.json` conditions as well as native ones, so the population it can fire on is larger than the one measured. No new measurement was taken". M5-0.5 stage 1 takes the measurement with the JSON stratum reported separately or as unmeasured; no build.
5. *#82*: #84 says "Needs a measurement before it is worth building; amending the specification to the three rungs that exist is an equally acceptable outcome"; §8 states: "What it needs before it is `ready-for-agent`, in #82's words: a measurement in the shape standing rule 4 requires — how often a diagnostic-driven repair would resolve an input the three built rungs leave as a judgement point — over the corpus in `research/corpus/m45-synthesis.json` through `just measure-synthesis`; if the rate justifies the rung, the bounded repair table and the fixture that gates it. Either building the rung or amending the preference order to the three rungs that exist closes it." M5-0.5 is that measurement in two stages with its decision rules; the prior (zero open judgement points over the M4.5 corpus) is stated beside it; the thresholds are new policy and are labelled so.
6. *Per-assertion provenance*: 13-m45 §14: "Per-assertion provenance would make `curate` sharper and is a prerequisite for any future `curate --apply`"; #84: "**Per-assertion provenance** would sharpen `curate` and is a prerequisite for any future `curate --apply`, which M4.5 deferred deliberately"; §8: "A per-assertion registry needs per-assertion digests written at generation time and is the prerequisite for any future `curate --apply`, which M4.5 deferred and this programme did not touch." All three agree; out of scope.
7. *The `moved` bullet*: #84's first comment: "The correction noted above is applied on master in `a52fbe9`: `docs/research/13-m45-exit-gate.md` §14 no longer carries the `moved` refusal as an open question, since §12 of the same document records it being answered inside the milestone. The five questions listed in this issue are what §14 now holds." At HEAD §14 carries five bullets and no `moved` bullet. Nothing to do.


