{
  "variable": {
    "tags": {
      "type": "list(string)",
      "validation": [
        {
          "condition": "%{ if length(var.tags) == 1 }true%{ else }false%{ endif }",
          "error_message": "tags must carry exactly one entry"
        }
      ]
    }
  },
  "resource": {
    "terraform_data": {
      "anchor": {
        "input": "${join(\",\", var.tags)}"
      }
    }
  },
  "output": {
    "anchor": {
      "value": "${terraform_data.anchor.output}"
    }
  }
}
