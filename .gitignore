.terraform/
*.tfstate
*.tfstate.backup
/dist/
/tf-mut
/.artifacts/
/.tools/
mise.local.toml
mise.local.lock

# Agent-managed worktrees live inside the checkout but are never repository content.
/.claude/worktrees/
